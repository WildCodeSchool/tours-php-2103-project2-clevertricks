{% extends 'layout.html.twig' %}

{% block header %}{% endblock %}

{% block content %}
<a id="home" href="/">Accueil</a>
<section class="tricksByCategory">
    <h3>Nos astuces dans la catégorie </h3>
        <ul>
            {% for trick in tricksByCategory %}
                <li> 
                    <a href="#">{{trick.title}}</a> 
                </li>
            {% endfor %}
        </ul>
</section>
{% endblock %}